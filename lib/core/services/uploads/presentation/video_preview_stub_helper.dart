// تم تعليق هذا الملف - نستخدم رفع الصور فقط حالياً
/*
// Stub implementation for non-web platforms
import 'dart:typed_data';

String createBlobUrl(Uint8List bytes) {
  // On non-web platforms, we don't need blob URLs
  // This should never be called as we check kIsWeb before calling
  throw UnsupportedError('Blob URLs are only supported on web');
}

void revokeBlobUrl(String url) {
  // No-op on non-web platforms
}
*/
